#include <iostream>
#include <vector>

using namespace std;

bool includes(vector<int> vect, int val){
    int l(0), r(vect.size()-1), m;
    m = (l+r)/2;
    while(true){
        if(l>=r){
            return false;
        }
        
    }
}